# Wordstat 接入验收记录

日期：2026-09-22。

- 改造前67项 Python 测试通过；接入后83项 Python 测试通过，其中新增16项 Wordstat 与交接测试。
- 5项宇通 JavaScript 回归测试通过，包括九月、十月、跨年一月、动态行数及线索增长；Python 编译检查通过。
- 模拟100个种子，每个返回50个热门扩展词，仍完整请求100个种子；所有种子保留，每种子最多30个候选，最终600个词。同等相关性测试中，每个种子贡献5个扩展词；输入与返回顺序打乱后结果一致。
- 模拟滚动小时配额、进程重启、429、权限错误、超时、同任务并发锁及中断恢复，未消耗真实 API 配额。
- 模拟 AI 整理中途失败，成功批次复用，失败批次重试，Wordstat 成功查询未重复调用；AI 漏词、重复词、虚构词和额外搜索量字段被拒绝。
- 新旧流程人工断点、Excel/CSV 最终词表到否词/广告语的衔接、地区覆盖、空值与真实0、600种子限制均已测试。
- 两种审核表实际导出为 Excel 并回读：筛选表、A2冻结窗格、表头、中俄内容正确，搜索量1200/0/缺失保持类型与语义。预览图片已检查，无文字截断。
- 操作指南、README、架构说明、原项目日志交接清单及 Wordstat 技术说明已同步更新；当前文档相对链接检查通过。
- 旧实验、人工文件、历史报告及带指纹清单保留，不自动转为正式查询缓存；未修改真实宇通历史表。

验证日志与表格样本保存在 `outputs/_migration/2026-09-22/`。目录迁移脚本会在更名后再次执行全部 Python 测试、pip/统一入口检查与宇通八月输入检查；成功日志为 `python-tests-after-rename.log`，迁移过程不会提交或推送 Git。

本次验证使用模拟 API/AI 数据和本地表格。真实账号查询与模型分类尚未做在线验收；首次正式项目仍需检查实际返回和人工分类质量。

## 目录迁移状态

截至本次交付，目录仍为 `D:\sem自动化`。已授权的迁移命令被 Windows 拒绝访问 `.git`，移动内容已回退；没有改动权限、删除 Git 历史或留下部分项目副本。VS Code 的6个工作区相对路径任务已更新。更名后的回归检查尚未执行，待按 [项目更名操作](项目更名操作.md) 迁移后运行。
