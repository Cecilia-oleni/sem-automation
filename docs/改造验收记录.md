# 改造验收记录（2026-09-20）

## 完成的结构与入口

- 唯一根目录为 `D:\sem自动化 - 副本`，原内层代码与 Git 元数据提升至此；保留原提交历史和未提交修改，没有自动提交。
- VS Code 的解释器、终端目录和 `.env` 路径均指向根目录。虚拟环境从原内层提升，外层旧连接已解除。
- 物料、报告、AI、资料读取、API 和公共能力已拆分；有自动测试禁止基础层导入业务层、禁止两条业务流程相互导入。
- 正式入口为 `sem.py`；安琪旧入口为兼容包装。活动模块顶部包含用途与可复制命令。
- Wordstat 归入 `experiments/wordstat`，仅适配环境路径，缓存、人工备注及实验调用限制保留。固定批次脚本归档。
- 未来发布工作流的边界已记录；未实现任何广告上传。

## 测试与报告回归

- 改造前：59 项 Python 测试通过。
- 改造后：67 项 Python 测试、5 项 JavaScript 测试通过；Python 模块编译检查通过。
- 物料测试覆盖人工断点、保留/重跑、网站分支、注入路径以及 `dry-run` 禁止联网和写入。
- 通用凌宇七月：离线生成两份报告，对比原文件的单元格、公式、工作表顺序、合并区域、图表数量和打印范围，无差异。
- 安琪八月：使用原始 JSON，关闭翻译，生成三份报告；上述对比仅“说明”页生成时间变化，其他比较项一致。已修复离线重建丢失原译文的问题。
- 宇通八月：原始文件来自 `D:\Elena\9. 宇通客车\`。展示 3,891,435、点击 9,932、消耗 1,340.41 USD、筛选线索 86；关键展示区域数值与已有已验证报告一致。YTD 由当年月份源数据重新汇总，并校验数值页与公式页一致。
- 宇通同月：把候选表作为下一次输入，确认月份数量、块位置、国家数据不变，不重复追加。
- 九月、十月、跨年一月：使用内存历史表测试月份选择、YTD 公式来源、年度区域和重跑；增长到 2,000 条线索及历史行数超过 1,500 行的场景通过。缺历史期间、重复月份、歧义月份和缺表头有拒绝测试。这些是模拟边界测试，不是未来月份真实数据报告验收。
- 同步：候选篡改、源表变化、备份、模拟第二份替换失败、真实 Windows 文件占用及回滚测试通过。只操作临时文件，未同步真实宇通历史表。

## Excel 检查与限制

检查了保存后的工作簿，并渲染代表性展示区域。安琪和通用报告沿用现有版式；宇通新增年月文本后，对受影响表头增加换行/行高，避免日期和列名截断。

历史公式中中文工作表名称已补标准引用引号，防止导出重算引入 `#NAME?`。宇通模板 `Sheet1!Y25` 原有 1 个 `#VALUE!`，国家历史原始页原有 19 个错误单元格，均与源文件一致，未擅自修复；它们不属于本期计算区域。保存后的新增错误会阻止正式交付，既有错误记录在 `_internal/workbook_checks.json`。

此次没有进行付费模型调用或实际广告平台采集，也没有通过 Excel/WPS 桌面程序验证打印效果。新月份仍需要完整原始导出和明确的汇率、核对总数；不能直接复用八月的业务数值。

## 可追溯资料

`outputs/_migration/2026-09-20/` 保存改造前代码快照、Git 差异、目录移动清单、报告比较结果和同月重跑结果。旧数据保留于 `outputs/_archive`，未混入新的正式运行。开发过程中的失败/中间验收运行归入迁移记录，正常报告目录只保留最终验收版本。
